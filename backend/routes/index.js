 const users = require("./users.route");
 const products = require("./products.route");
 const orders = require("./orders.route")
 const carts = require("./carts.route")

 module.exports = {users, products, orders, carts};